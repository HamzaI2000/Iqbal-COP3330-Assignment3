This will help read the data from Scanner object to the ArrayList
Define the function to print the names in the file
Now, you will a FileWriter for the output file
Write  a header of the output and go through with it, then write the output 
Create an scanner object for read while creating an ArrayList to store names
Call the readname() function and sort it as a list
Close scanner object as required by the data of the output file
